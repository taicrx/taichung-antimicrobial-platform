const DATA=window.ANTIMICROBIAL_APP_DATA;
const state={mode:'drug',query:'',selectedConcept:null,selectedProduct:null,group:'all',className:'all',alpha:'all',sort:'generic',modality:'HD',limit:20};
const $=s=>document.querySelector(s);const esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const norm=(v='')=>String(v).toLowerCase().replace(/[^a-z0-9\u4e00-\u9fff]+/g,'');
const statusLabel=s=>({allowed:'可使用',not_recommended:'不建議使用',no_data:'無資料 N/D',compatible:'相容',conditional:'條件式相容',incompatible:'不相容',uncertain:'不確定',instruction:'依指示'}[s]||s);
const groupLabel=g=>({all:'全部',Antibacterial:'抗細菌',Antifungal:'抗黴菌',Antiviral:'抗病毒','Anti-TB':'抗結核'}[g]||g);
function formatDateTime(v){if(!v)return'N/D';const d=new Date(v);return Number.isNaN(d.getTime())?v:new Intl.DateTimeFormat('zh-TW',{timeZone:'Asia/Taipei',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',hour12:false}).format(d)}
function initialOf(d){return(d.genericName||'#').trim().charAt(0).toUpperCase()}function alphaMatch(c,b){if(b==='all')return true;const n=c.charCodeAt(0);return b==='A-F'?n>=65&&n<=70:b==='G-L'?n>=71&&n<=76:b==='M-R'?n>=77&&n<=82:b==='S-Z'?n>=83&&n<=90:true}
function searchable(d){return norm([d.genericName,d.className,d.mechanism,d.group,...d.aliases,...d.products.flatMap(p=>[p.brandName,p.hospitalDrugId,p.procedureCode,p.strength])].join(' '))}
function filtered(){let x=DATA.drugs.slice();if(state.group!=='all')x=x.filter(d=>d.group===state.group);if(state.className!=='all')x=x.filter(d=>d.className===state.className);if(state.alpha!=='all')x=x.filter(d=>alphaMatch(initialOf(d),state.alpha));const q=norm(state.query);if(q)x=x.filter(d=>searchable(d).includes(q));x.sort((a,b)=>state.sort==='brand'?(a.products[0]?.brandName||'').localeCompare(b.products[0]?.brandName||'','en'):state.sort==='class'?(a.className||'').localeCompare(b.className||'','en')||a.genericName.localeCompare(b.genericName,'en'):a.genericName.localeCompare(b.genericName,'en'));return x}
function renderGroups(){const groups=['all',...new Set(DATA.drugs.map(d=>d.group))];$('#groupFilters').innerHTML=groups.map(g=>`<button class="filter-chip ${state.group===g?'active':''}" data-g="${esc(g)}">${esc(groupLabel(g))}</button>`).join('');$('#groupFilters').querySelectorAll('button').forEach(b=>b.onclick=()=>{state.group=b.dataset.g;state.className='all';state.limit=20;renderDrugMode()})}
function renderClasses(){const pool=DATA.drugs.filter(d=>state.group==='all'||d.group===state.group);const cls=[...new Set(pool.map(d=>d.className))].sort();$('#classFilter').innerHTML=`<option value="all">全部藥理分類</option>${cls.map(c=>`<option value="${esc(c)}">${esc(c)}</option>`).join('')}`;$('#classFilter').value=cls.includes(state.className)?state.className:'all'}
function listHTML(items){let current='';return items.map(d=>{const l=initialOf(d),head=l!==current?`<div class="letter-heading">${esc(l)}</div>`:'';current=l;const available=d.adultDoses.some(x=>x.dose_display!=='N/D');return head+`<button class="result-item ${state.selectedConcept===d.conceptId?'active':''}" data-id="${esc(d.conceptId)}"><div class="generic">${esc(d.genericName)}</div><div class="brands">${esc(d.products.map(p=>p.brandName).join('、'))}</div><div class="meta"><span class="badge ${available?'':'nd'}">${available?'有劑量資料':'部分資料 N/D'}</span><span>${esc(d.className)}</span></div></button>`}).join('')}
function renderDrugList(){const all=filtered(),shown=all.slice(0,state.limit);$('#sidebarTitle').textContent='藥品';$('#resultCount').textContent=`符合 ${all.length} 種成分／${all.reduce((n,d)=>n+d.products.length,0)} 個品項`;$('#resultList').innerHTML=listHTML(shown)||'<div class="backlog-box">沒有符合的藥品。</div>';$('#showMore').style.display=all.length>state.limit?'block':'none';$('#resultList').querySelectorAll('[data-id]').forEach(b=>b.onclick=()=>{state.selectedConcept=b.dataset.id;const d=DATA.drugs.find(x=>x.conceptId===state.selectedConcept);state.selectedProduct=d?.products[0]?.hospitalDrugId||null;renderDrugDetail();renderDrugList();if(innerWidth<1050)$('#content').scrollIntoView({behavior:'smooth'})})}
function section(t,b,o=true){return`<section class="section ${o?'':'collapsed'}"><button class="section-title"><span>${t}</span><span>⌄</span></button><div class="section-body">${b}</div></section>`}
function refBadges(text){if(!text)return'';return`<div class="source-row">${String(text).split(',').filter(Boolean).map(x=>`<span class="source-badge">${esc(x.trim())}</span>`).join('')}</div>`}
function routeCards(d,hid){const rows=d.administration.filter(x=>x.hospital_drug_id===hid);return`<div class="route-grid">${['IV push','IM','IV infusion'].map(route=>{const a=rows.find(x=>x.route===route)||{status:'no_data'};return`<div class="route-card ${esc(a.status)}"><h3>${route}</h3><div class="route-status">${esc(statusLabel(a.status))}</div><div class="route-detail">${esc(a.administration_instruction||'')}</div>${a.maximum_rate_display?`<div class="route-detail">最大速率：${esc(a.maximum_rate_display)}</div>`:''}${refBadges(a.reference_display)}</div>`}).join('')}</div>`}
function doseSection(d){return section('一般成人與腎功能劑量',`<div class="data-grid">${d.adultDoses.map(x=>`<div class="info-card"><div class="label">${esc(x.indication)}｜${esc(x.renal_category)}</div><h4>${esc(x.dose_display)}</h4>${x.loading_dose_display?`<p><strong>Loading：</strong>${esc(x.loading_dose_display)}</p>`:''}${x.special_note?`<p>${esc(x.special_note)}</p>`:''}<p class="label">${esc(x.review_status)}｜${esc(x.source_id)}</p></div>`).join('')}</div>`)}
function rrtSection(d){const tabs=['HD','PD','CVVH','CVVHDF','ECMO'];const x=d.rrtDoses.find(y=>y.modality===state.modality)||{};return section('特殊族群劑量（HD／PD／CRRT／ECMO）',`<div class="pop-tabs">${tabs.map(t=>`<button class="pop-tab ${state.modality===t?'active':''}" data-mod="${t}">${t}</button>`).join('')}</div><div class="info-card"><div class="label">${esc(state.modality)}｜${esc(x.status||'no_data')}</div><h4>${esc(x.dose_display||'N/D')}</h4>${x.usual_dose_display?`<p>一般劑量：${esc(x.usual_dose_display)}</p>`:''}${x.note?`<p>${esc(x.note)}</p>`:''}${refBadges(x.reference_text)}</div>`)}
function compBlock(rows){return`<div class="compat-grid">${rows.map(x=>`<div class="compat-item ${esc(x.status)}"><strong>${esc(x.solution_code)}｜${esc(statusLabel(x.status))}</strong>${x.condition_display?`<div class="condition">${esc(x.condition_display)}</div>`:''}${refBadges(x.reference_display)}</div>`).join('')}</div>`}
function preparationSection(d,hid){const comp=d.compatibility.filter(x=>x.hospital_drug_id===hid),stb=d.stability.filter(x=>x.hospital_drug_id===hid);return section('調製、稀釋液相容性與保存時效',`<h4>重組液</h4>${compBlock(comp.filter(x=>x.phase==='Reconstitution'))}<h4 style="margin-top:16px">稀釋液</h4>${compBlock(comp.filter(x=>x.phase==='Dilution'))}<h4 style="margin-top:16px">保存時效</h4><div class="data-grid">${stb.map(x=>`<div class="info-card"><div class="label">${esc(x.storage_condition)}｜${esc(x.status)}</div><h4>${esc(x.duration_display||'N/D')}</h4>${x.special_condition?`<div class="stability-note">${esc(x.special_condition)}</div>`:''}${refBadges(x.reference_display)}</div>`).join('')}</div>`)}
function pkSection(d){return section('PK／PD與透析特性',`<div class="data-grid">${d.pkpd.map(x=>`<div class="info-card"><div class="label">${esc(x.parameter_name)}</div><h4>${esc(x.value_display)}</h4><p>${esc(x.component_scope)}</p></div>`).join('')}</div>`,false)}
function dxSection(d){return section('本院診斷群指引中的連結',`<div class="dx-list">${d.diagnoses.length?d.diagnoses.map(x=>`<div class="dx-card"><h3>${esc(x.diagnosis)} ${x.severity?`<span class="badge">${esc(x.severity)}</span>`:''}</h3><div class="dx-row"><div class="dx-label">首選</div><div>${esc(x.firstLine)}</div></div><div class="dx-row"><div class="dx-label">替代</div><div>${esc(x.alternative||'—')}</div></div></div>`).join(''):'<div class="backlog-box">目前診斷群指引未連結此成分。</div>'}</div>`,false)}
function renderDrugDetail(){const d=DATA.drugs.find(x=>x.conceptId===state.selectedConcept);if(!d){$('#content').innerHTML='<div class="empty-state"><div><h2>選擇藥品開始查詢</h2><p>可搜尋學名、商品名、處置代碼、縮寫，或使用分類與A–Z。</p></div></div>';return}if(!state.selectedProduct||!d.products.some(p=>p.hospitalDrugId===state.selectedProduct))state.selectedProduct=d.products[0]?.hospitalDrugId;const p=d.products.find(x=>x.hospitalDrugId===state.selectedProduct)||d.products[0];$('#content').innerHTML=`<div class="drug-header"><div class="drug-title"><span class="badge">${esc(groupLabel(d.group))}｜${esc(d.className)}</span><h2>${esc(d.genericName)}</h2><p>${esc(d.mechanism)}</p><div class="product-picker"><label>院內品項<select id="productSelect" class="product-select">${d.products.map(x=>`<option value="${esc(x.hospitalDrugId)}" ${x.hospitalDrugId===p.hospitalDrugId?'selected':''}>${esc(x.brandName)}｜${esc(x.hospitalDrugId)}｜${esc(x.strength||'N/D')}</option>`).join('')}</select></label></div></div><div class="review-box"><strong>${esc(p.brandName)}｜${esc(p.hospitalDrugId)}</strong><br>資料版本：${esc(DATA.meta.datasetVersion)}<br>資料發布：${esc(formatDateTime(DATA.meta.datasetPublishedAt))}</div></div>${routeCards(d,p.hospitalDrugId)}${doseSection(d)}${rrtSection(d)}${preparationSection(d,p.hospitalDrugId)}${pkSection(d)}${dxSection(d)}`;$('#productSelect').onchange=e=>{state.selectedProduct=e.target.value;renderDrugDetail()};$('#content').querySelectorAll('.section-title').forEach(b=>b.onclick=()=>b.parentElement.classList.toggle('collapsed'));$('#content').querySelectorAll('[data-mod]').forEach(b=>b.onclick=()=>{state.modality=b.dataset.mod;renderDrugDetail()})}
function renderDrugMode(){$('#filterPanel').style.display='';renderGroups();renderClasses();$('#alphaFilter').value=state.alpha;$('#sortFilter').value=state.sort;renderDrugList();renderDrugDetail()}
function renderDiagnosis(){const q=norm(state.query),list=DATA.diagnoses.filter(d=>!q||norm([d.diagnosis,d.severity,d.first_line_regimen,d.alternative_regimen].join(' ')).includes(q));$('#filterPanel').style.display='none';$('#showMore').style.display='none';$('#sidebarTitle').textContent='診斷群';$('#resultCount').textContent=`符合 ${list.length} 筆`;$('#resultList').innerHTML=list.map(d=>`<button class="result-item" data-dx="${esc(d.guideline_id)}"><div class="generic">${esc(d.diagnosis)}</div><div class="brands">${esc(d.severity||'')}</div></button>`).join('');$('#content').innerHTML=`<div class="drug-header"><div class="drug-title"><span class="badge warn">版本待確認</span><h2>本院經驗性抗生素指引</h2><p>檔名與文件內文版本不一致，正式核定前請以院內公告為準。</p></div></div><div class="dx-list" style="margin-top:17px">${list.map(d=>{const links=(d.links||[]).filter(l=>l.link_type==='Drug concept').map(l=>{const z=DATA.drugs.find(x=>x.conceptId===l.linked_id);return z?`<button class="link-drug" data-drug="${esc(z.conceptId)}">${esc(z.genericName)}</button>`:''}).join('');return`<div class="dx-card" id="${esc(d.guideline_id)}"><h3>${esc(d.diagnosis)} ${d.severity?`<span class="badge">${esc(d.severity)}</span>`:''}</h3><div class="dx-row"><div class="dx-label">首選</div><div>${esc(d.first_line_regimen)}</div></div><div class="dx-row"><div class="dx-label">替代</div><div>${esc(d.alternative_regimen||'—')}</div></div>${links}</div>`}).join('')}</div>`;$('#resultList').querySelectorAll('[data-dx]').forEach(b=>b.onclick=()=>document.getElementById(b.dataset.dx)?.scrollIntoView({behavior:'smooth'}));$('#content').querySelectorAll('[data-drug]').forEach(b=>b.onclick=()=>{state.mode='drug';state.selectedConcept=b.dataset.drug;const d=DATA.drugs.find(x=>x.conceptId===state.selectedConcept);state.selectedProduct=d?.products[0]?.hospitalDrugId;render()})}
function renderSpecial(){const mods=['HD','PD','CVVH','CVVHDF','ECMO'];$('#filterPanel').style.display='none';$('#showMore').style.display='none';$('#sidebarTitle').textContent='特殊族群';$('#resultCount').textContent='5 modalities';$('#resultList').innerHTML=mods.map(m=>`<button class="result-item ${state.modality===m?'active':''}" data-mod="${m}"><div class="generic">${m}</div></button>`).join('');const rows=DATA.drugs.map(d=>{const x=d.rrtDoses.find(s=>s.modality===state.modality);return`<div class="info-card"><div class="label">${esc(d.genericName)}</div><h4>${esc(x?.dose_display||'N/D')}</h4>${x?.note?`<p>${esc(x.note)}</p>`:''}<button class="link-drug" data-drug="${esc(d.conceptId)}">開啟完整藥品頁</button></div>`}).join('');$('#content').innerHTML=`<div class="drug-header"><div class="drug-title"><span class="badge">特殊族群比較</span><h2>${esc(state.modality)}劑量總覽</h2><p>無資料固定顯示N/D，不代表不需調整。</p></div></div><div class="data-grid" style="margin-top:17px">${rows}</div>`;$('#resultList').querySelectorAll('[data-mod]').forEach(b=>b.onclick=()=>{state.modality=b.dataset.mod;renderSpecial()});$('#content').querySelectorAll('[data-drug]').forEach(b=>b.onclick=()=>{state.mode='drug';state.selectedConcept=b.dataset.drug;const d=DATA.drugs.find(x=>x.conceptId===state.selectedConcept);state.selectedProduct=d?.products[0]?.hospitalDrugId;render()})}
function activeMode(){document.querySelectorAll('.mode-tab').forEach(b=>b.classList.toggle('active',b.dataset.mode===state.mode))}function render(){activeMode();if(state.mode==='drug')renderDrugMode();else if(state.mode==='diagnosis')renderDiagnosis();else renderSpecial()}
document.querySelectorAll('.mode-tab').forEach(b=>b.onclick=()=>{state.mode=b.dataset.mode;state.limit=20;render()});$('#search').addEventListener('input',e=>{state.query=e.target.value;$('#clearSearch').style.display=state.query?'block':'none';state.limit=20;render()});$('#clearSearch').onclick=()=>{state.query='';$('#search').value='';$('#clearSearch').style.display='none';state.limit=20;render()};$('#classFilter').onchange=e=>{state.className=e.target.value;state.limit=20;renderDrugMode()};$('#alphaFilter').onchange=e=>{state.alpha=e.target.value;state.limit=20;renderDrugMode()};$('#sortFilter').onchange=e=>{state.sort=e.target.value;state.limit=20;renderDrugMode()};$('#showMore').onclick=()=>{state.limit+=20;renderDrugList()};$('#mobileFilterToggle').onclick=()=>$('#filterPanel').classList.toggle('open');
const products=DATA.drugs.reduce((n,d)=>n+d.products.length,0);$('#totalStats').textContent=`收錄 ${products} 個現行院內品項／${DATA.drugs.length} 種成分`;$('#publishedMeta').textContent=`資料發布：${formatDateTime(DATA.meta.datasetPublishedAt)}`;$('#versionMeta').textContent=`資料版本：${DATA.meta.datasetVersion}｜平台版本：${DATA.meta.appVersion}`;$('#alertText').textContent=DATA.qualityAlerts?.[0]?.description||DATA.meta.disclaimer;render();